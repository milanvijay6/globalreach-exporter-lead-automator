{
  "name": "globalreach-web-server",
  "version": "1.0.0",
  "description": "Web server for hosting GlobalReach Exporter Lead Automator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "copy-build": "node copy-build.js",
    "build": "echo 'Build the React app first, then run: npm run copy-build'"
  },
  "keywords": ["globalreach", "web-server"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2",
    "compression": "^1.7.4",
    "helmet": "^8.1.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

